import 'package:flutter/material.dart';

class Guidedb extends StatelessWidget {
  const Guidedb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
